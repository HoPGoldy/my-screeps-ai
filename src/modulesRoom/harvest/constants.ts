/**
 * miner 的矿物采集上限
 * 当房间中的对应矿物资源多于这个值时，miner 将不再继续采矿
 */
export const MINE_LIMIT = 100000

/**
 * 默认的元素矿采集单位角色名
 */
export const DEFAULT_MINER_ROLE = 'miner'

/**
 * 默认的能量矿采集单位角色名
 */
export const DEFAULT_HARVESTER_ROLE = 'harvester'

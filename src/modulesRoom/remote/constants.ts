/**
 * 签名单位的默认角色名
 */
export const DEFAULT_SIGNER_ROLE = 'signer'

/**
 * 外矿采集单位的默认角色名
 */
export const DEFAULT_REMOTE_HARVESTER_ROLE = 'remoteHarvester'

/**
 * 预定单位的默认角色名
 */
export const DEFAULT_RESERVER_ROLE = 'reserver'

/**
 * 占领单位的默认角色名
 */
export const DEFAULT_CLAIMER_ROLE = 'claimer'

/**
 * 新房协助单位的默认角色名
 */
export const DEFAULT_REMOTE_HELPER = 'remoteHelper'
